# `manyterm`

A cross-platform terminal input handler, in replacement for `crossterm`'s input handler. There were many issues with it that hindered the development of my previous editor, so I created a replacement.