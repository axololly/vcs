- More commit commands:
    - `trash`

- Add tags like `git tag` to immutably address certain commits

- Document everything that's currently in the repository

- Add the ability to push and pull from remotes, as well as set up servers in a single command like `fossil server`
